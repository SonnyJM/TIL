#print()

#를 하면 오류가 뜨지 않으면서 아무 값도 출력하지 않는데,
#정말 아무것도 치지 않는 것 대신 None을 사용하는 이유가 있는지?

print() # 공백과 줄바꿈이 들어있는 값. 공백값이 출력됨. 값이 있음.
print(None)

'''
프로그래밍 언어 학습시 가장 많이 헷갈려 하는 것
0 -> 값이 있나요 없나요? 있음.
'' -> 값이 있음.
False -> 값 있음
[]. {} -> 값 있음.
None : 존재하지 않음.
'''